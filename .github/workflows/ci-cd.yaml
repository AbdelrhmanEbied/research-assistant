name: CI/CD

on:
    push:
        branches: [master]
        tags: ["v*"]
    pull_request:

concurrency:
    group: ${{ github.workflow }}-${{ github.ref }}
    cancel-in-progress: true

jobs:
    test:
        name: Lint & Test
        runs-on: ubuntu-latest

        steps:
            - uses: actions/checkout@v5

            - name: Install uv
              uses: astral-sh/setup-uv@v5
              with:
                  python-version: "3.14"

            - name: Install dependencies
              run: uv sync --frozen

            - name: Lint
              run: uv run ruff check .

            - name: Format check
              run: uv run ruff format --check .

            - name: Run tests
              run: uv run pytest

    rag-eval:
        name: RAG Evaluation
        needs: test
        runs-on: ubuntu-latest

        steps:
            - uses: actions/checkout@v5

            - name: Install uv
              uses: astral-sh/setup-uv@v5
              with:
                  python-version: "3.14"

            - name: Install dependencies
              run: uv sync --frozen

            - name: Run retrieval evaluation
              run: |
                  uv run python scripts/evaluate_rag.py \
                      --dataset evaluation/sample_data/example.jsonl \
                      --corpus evaluation/sample_data/corpus \
                      --search-types hybrid \
                      --k 3 \
                      --output rag-eval-report.json

            - name: Check retrieval quality
              run: |
                  python - <<'PY'
                  import json

                  report = json.load(open("rag-eval-report.json"))
                  metrics = report["retrieval"]["hybrid/no-rerank:document"]
                  assert metrics["hit_rate"] >= 0.7, f"hit_rate too low: {metrics}"
                  assert metrics["mrr"] >= 0.7, f"mrr too low: {metrics}"
                  print("RAG retrieval quality OK:", metrics)
                  PY

    build-and-push:
        name: Build & Push Docker Image
        needs: [test, rag-eval]
        if: github.event_name == 'push' && github.ref == 'refs/heads/master'
        runs-on: ubuntu-latest

        permissions:
            contents: read
            packages: write

        steps:
            - uses: actions/checkout@v5

            - name: Set up Docker Buildx
              uses: docker/setup-buildx-action@v3

            - name: Log in to GHCR
              uses: docker/login-action@v3
              with:
                  registry: ghcr.io
                  username: ${{ github.actor }}
                  password: ${{ secrets.GITHUB_TOKEN }}

            - name: Generate Docker tags
              id: meta
              uses: docker/metadata-action@v5
              with:
                  images: ghcr.io/${{ github.repository }}
                  tags: |
                      type=sha,prefix=,format=short
                      type=raw,value=latest,enable={{is_default_branch}}
                      type=semver,pattern={{version}}

            - name: Build and push
              uses: docker/build-push-action@v6
              with:
                  context: .
                  push: true
                  build-args: |
                      BAKE_MODELS=true
                  tags: ${{ steps.meta.outputs.tags }}
                  labels: ${{ steps.meta.outputs.labels }}
                  cache-from: type=gha
                  cache-to: type=gha,mode=max

    deploy:
        name: Deploy to Kubernetes
        needs: build-and-push
        if: github.event_name == 'push' && github.ref == 'refs/heads/master'
        runs-on: self-hosted

        permissions:
            contents: read

        steps:
            - uses: actions/checkout@v5

            - name: Install kubectl
              uses: azure/setup-kubectl@v4

            - name: Deploy
              run: |
                  IMAGE="ghcr.io/${GITHUB_REPOSITORY,,}:${GITHUB_SHA::7}" bash scripts/deploy.sh
